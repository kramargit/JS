1. оформляем структуру html
	создается общий контейнер для слайдера
	создаются контейнры для каждого из элементов содержимого слайдера
	создаются кнопки влево и вправо + к ним крепится обработчик события с параметрами -1 и +1 соответственно
	если требуется создаются точки(маркеры) для навигации по элементам слайдера
2. оформляем стили css
3. пишем js код
	задаем состояние по умолчанию, т.е. что будет показывать слайдер сначала
	принимаем обработчик события на кнопках вправо и влево
	передаем параметр в функцию, которая будет опеределять номер текущего слайда
	функция работает по принципу: сначала все элементы не отбражаются, после отображется выбранный